'use client';

export { useCountDown, useLatest } from '@go-tech/hooks';
/**
 * @deprecated `@go-tech-frontend/lib` has been split into platform-agnostic
 * core packages. Import from `@go-tech/core-utils` (cn, truncate, formatNumber,
 * type guards) and `@go-tech/hooks` (useCountDown, useLatest) instead.
 * This module is a temporary back-compat shim and will be removed.
 */
export * from '@go-tech/core-utils';
